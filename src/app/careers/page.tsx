import { PlaceholderPageTemplate } from "@/components/landing/PlaceholderPageTemplate";

export const metadata = { title: "Careers — CareerMaxxer" };
export default function CareersPage() {
  return <PlaceholderPageTemplate title="Careers" />;
}
