import { PlaceholderPageTemplate } from "@/components/landing/PlaceholderPageTemplate";

export const metadata = { title: "Blog — CareerMaxxer" };
export default function BlogPage() {
  return <PlaceholderPageTemplate title="Blog" />;
}
