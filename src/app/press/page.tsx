import { PlaceholderPageTemplate } from "@/components/landing/PlaceholderPageTemplate";

export const metadata = { title: "Press — CareerMaxxer" };
export default function PressPage() {
  return <PlaceholderPageTemplate title="Press" />;
}
